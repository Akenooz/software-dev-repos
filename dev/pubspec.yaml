name: dev
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  intl: ^0.17.0  # Keep the existing constraint

  table_calendar: ^3.0.8  # Downgrade the table_calendar version

  google_sign_in: ^5.2.1  # Latest version as of now
  googleapis: ^11.4.0  # Use a version that supports null safety
  googleapis_auth: ^1.4.1  # Use the latest compatible version

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/google.png
    - lib/images/apple.png
